import 'jest';
// import { Repository } from '../../templates';

describe('BuildFunctionTask:', () => {
	beforeAll(() => {
		jest.mock('dockerode');
		jest.mock('execa');
		jest.mock('../../templates');
	});

	it('placeholder', () => {
		//
	});
});
